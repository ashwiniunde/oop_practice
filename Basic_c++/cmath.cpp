#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    cout<<"Square root of 64 is "<<sqrt(64);
    cout<<"Round 2.8 to the nearest of: "<<round(2.8);
    cout<<"The natural log of 2 is : "<<log(2);
    return 0;
}